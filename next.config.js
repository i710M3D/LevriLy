/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  images: {
    domains: ["images.unsplash.com","www.figma.com","labs.openai.com"],
  },
};
